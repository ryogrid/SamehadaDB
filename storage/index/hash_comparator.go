package index

/**
 * HashComparator is for comparing hash_t, it returns:
 *  < 0 if lhs < rhs
 *  > 0 if lhs > rhs
 *  = 0 if lhs = rhs
 */
// TODO: (SDB) need port HashComparator class
/*
class HashComparator {
	public:
	 inline int operator()(const hash_t lhs, const hash_t rhs) { return lhs < rhs ? -1 : (lhs > rhs ? 1 : 0); }
};
*/
